# Polynomial systems benchmark

This repository powers [Polynomial_systems_benchmark](https://github.com/sumiya11/Polynomial_systems_benchmark).

## Build the website locally

Create and populate a virtual environment if needed:

```text
python -m venv venv
python -m pip install -r requirements.txt
```

Then build the static site:

```text
python website/build.py
```
